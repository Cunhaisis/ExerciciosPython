# Faça um programa que peça 10 números inteiros, calcule e mostre a quantidade de números pares e a quantidade de números
# impares.

contador = 0
par = 0
impar = 0

for c in range(1, 11):
    x = int(input('Digite um numero inteiro:'))
    if c % 2 == 0:
        par = par + 1
else:
    impar = impar + 1

print('o numeros pares digitados formam: {}'.format(par))
print('Os numeros impares digitador foram {}'.format(impar))
